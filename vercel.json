{
  "buildCommand": "cd frontend/repz && npm ci && npm run build -- --configuration production",
  "outputDirectory": "frontend/repz/dist/repz/browser",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
