name: CD - Ambiente Produção

on:
  push:
    branches: [ "main" ]

jobs:
  performance-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Load Test (RNF10)
        run: |
          echo "Validando suporte a 500 usuários simultâneos..."
          # Aqui você rodará o teste de carga (ex: npx k6 run performance.js)

  deploy-prod:
    needs: performance-test
    runs-on: ubuntu-latest
    environment:
      name: production # Ativa aprovação manual obrigatória
    steps:
      - name: Deploy com Rollback
        run: |
          echo "Iniciando deploy em produção após aprovação..."
          # Se o deploy falhar, o sistema mantém a versão anterior funcional